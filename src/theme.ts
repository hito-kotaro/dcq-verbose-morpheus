import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#252F3D',
      contrastText: '#fff',
    },
    secondary: {
      main: '#FA9900',
      contrastText: '#fff',
    },
    text: {
      primary: '#6F7073',
    },
  },
})
